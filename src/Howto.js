import React, {useTranslation} from 'react';

const Howto = () => {
    const {t} = useTranslation();
    return (
        <div>
            <h1>Howto</h1>
        </div>
    );
};

export default Howto;
