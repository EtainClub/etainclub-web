import React, {useTranslation} from 'react';

const Stories = () => {
    const {t} = useTranslation();
    return (
        <div>
            <h1>Stories</h1>
        </div>
    );
};

export default Stories;
